## Known Limitation — Phase 1 model
Trained and evaluated on the same 13 rows (one per district, single 2018
flood season) with no train/test split. Reported "1.00 accuracy" reflects
memorization, not generalization. Phase 2 will address this via proper
train/test splitting and, likely, expanding the training data beyond a
single season.