# ADR-001: FastAPI over Flask
FastAPI chosen for built-in Pydantic validation, automatic OpenAPI docs
(`/docs`), and native async support — reduces boilerplate versus Flask
for a project needing request validation and a dashboard-facing API.